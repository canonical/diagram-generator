# Abandoned VCF support-flow experiment

This branch preserves an abandoned VCF/Ubuntu support-flow diagram experiment for
historical reference only.

- Do not merge this branch into `main`.
- Do not use the YAML as a supported preview-editor input or product example.
- Do not cherry-pick the diagram into another feature branch.
- The experiment is known not to work and has no supported implementation,
  validation, or maintenance path.

The source is committed only to prevent accidental data loss and to make its
abandoned status explicit.
