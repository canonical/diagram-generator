

besides the language convo, theres als oenablement


"This diagram is too complex"
   * Mitigation Guidelines how to make it more simple. Support thought process. Doesnt have to be ai, can be examples
   * Examples - how the visual system works, communicative qualities
   * domain examples


   # Open questions
    * which limitations shouldbe hardvs soft
    * what types do engineers (excalidraw, lowest priority audience)
    * documentation users (much more important audience) - but written by engineers most often
    * TA's job is to define priorities. If we convince TAs 
    * Public/production ready facing vs own use - we're targetting public facing


    # Steps
    # 1 Agree on what is in scope
    # 2: Pick where we start - PMs natural first target group
    # Complexity bucketing
    # Pipeline (input/process/output)
    # Control of variation in output
    ## Visual grammer is there;
    ## Conceptual layer is missing; Map semantic concepts (e.g. in flowchsrts)
    ## Rules for composing (compositor); FOr example, current failures: overlaps between shapes. Nesting boxes, overlap boxes, grouping - representing strong vs wee krelationships
    # Relationship types: multiple belonging, overlap, allowed/disallowed conections (ven diag image from @Hartmut)
    # Interactivity (measured approach, not overkill full d3) THsts why people use mermaid - programmatically allows them 

    # Standard for diagrams ("What is the standard we can use in documentation") D2 - like mermaid but more complex - mermaid is the current standard, but way too limited

    # ALternative to mermaid - sphynx plugin? THe requirement: TAs should be able to go and make changes; needs to be patcheable.  Teodora from TAs; a bit of an outlier. 

    ## Political thing with TAs: not using screenshots, avoid things that change often.

    # Point it to a page, then ai suggests which passages to do as diagrams

