# Opus adversarial review request — spec 080 implementation

Review the completed implementation of spec 080 adversarially. Treat checked
tasks and claimed validation as assertions to disprove, not as evidence by
themselves.

## Mandatory workspace and output

- Worktree:
  `H:\WSL_dev_projects\diagram-generator-worktrees\080-renderable-interchange-import`
- Branch: `feat/080-renderable-interchange-import`
- Rebased dependency base: `f0f440f` (`fix(075): surface opened folder navigation`)
- Original spec-075 equivalent: `d7b4b67` on
  `feat/075-preview-folder-workspaces`
- Spec-080 delta: `git diff f0f440f...HEAD`
- Original findings:
  `docs/spec-reviews/opus-adversarial-review-findings-2026-07-18-renderable-interchange-import.md`

**Do not leave the review only in chat.** Write the complete review, including a
verdict even if no defects are found, to this exact file:

`H:\WSL_dev_projects\diagram-generator-worktrees\080-renderable-interchange-import\docs\spec-reviews\opus-adversarial-review-findings-2026-07-18-spec-080-implementation.md`

Create that file in the worktree. Chat may contain only a short pointer to it.

## Branch verification

Before reviewing:

1. Verify the current worktree and branch.
2. Record `HEAD`, verify `f0f440f` is the spec-080 dependency boundary, and
   record the merge-base with `main`.
3. Review `f0f440f...HEAD` separately from its rebased spec-075 dependency.
   The dependency was replayed onto current `main`; do not use the old branch
   name as a three-dot diff base. Flag any accidental unrelated change or
   ownership regression.

## Required adversarial audit

Read `AGENTS.md`, `AGENT-INBOX.md`, `docs/agent-index.md`, `DIAGRAM.md`, the
original findings, and the complete
`specs/080-renderable-interchange-import/` package. Then inspect the
implementation and attempt to falsify all of the following:

1. Every original finding F-C1 through F-L2 is actually resolved, rather than
   hidden by a narrower test or a different entry point.
2. Structural, invalid, and type loss blocks before every write in non-strict
   and strict modes: preview server root, local-folder mirror, Mermaid CLI, and
   D2 CLI. HTTP errors must retain the structured blocked summary, and the UI
   must never report success or navigate after a blocked import.
3. Mermaid uses bounded tokenization, typed IR, and lowering—not a new
   regex-per-line or legacy-JavaScript ownership seam. Exercise inline and
   chained declarations, implicit refinement, fan-out/fan-in, semicolons,
   shapes, labels/connectors, nested subgraphs, cross-container edges,
   self-loops, parallel edges, cycles, disconnected graphs, malformed input,
   oversized input, and adversarial labels.
4. Top-level and local `LR`, `TB`/`TD`, `RL`, and `BT` survive
   import → serialize → compile/reload. Confirm that reverse direction reaches
   ELK as `LEFT`/`UP`; an axis-only field must not silently replace orientation.
5. Engine selection reads declared capabilities and makes a truthful,
   deterministic choice. Verify flat compatible DAGs use v3, while
   reverse/cross-container/cyclic/fan-in cases select ELK layered, and that
   `meta.layout_engine` persists through both workspace modes. Try constructs
   that neither engine can faithfully render and confirm they block.
6. Supported Mermaid and D2 fill/border mappings are faithful; unsupported
   style, markdown, HTML, class, icon, variable, SQL, and edge-attribute cases
   are either named visual downgrades or blocking type/structural diagnostics as
   required by the capability matrix.
7. D2 genuinely shares the loss gate and covers chained edges, all four
   directions, class/direct styles, missing references, and malformed
   edge-shaped syntax. Look for accidental Mermaid-only semantics.
8. Server-root and local-folder tests really exercise
   import → persist → reload and assert topology, containment, orientation, and
   engine—not merely a successful response. Confirm blocked local imports never
   mirror a file.
9. The three corpus YAML fixtures were generated by the importer, include
   verifiable source path/SHA-256 provenance, compile, and exercise meaningful
   compound topology. Check that tests cannot pass on empty or flattened output.
10. The capability matrix has no optimistic classification, missing construct,
    or test citation that does not actually prove the row. Check spec, plan,
    tasks, validation, user docs, spec-028 compatibility notes, catalog, queue,
    and inbox for contradictions.
11. Browser bundle freshness, the spec-046 TypeScript ownership ratchet,
    source-artifact cleanup, no-new-Python ratchet, input bounds, and the
    two-second 1,000-edge budget are real and reproducible.

Search for future-facing gaps too: combinations of otherwise supported syntax,
diagnostic duplication/explosion, identifier collisions, escaping, Unicode,
comments/frontmatter, nested local directions, class-order conflicts, and
capability changes. Do not limit the review to existing tests.

## Validation to rerun

At minimum run:

```powershell
npm --prefix packages/layout-engine test
npm --prefix packages/layout-engine run build:browser
npm --prefix apps/preview test
npm --prefix apps/preview run build
npm run clean:src-artifacts
node scripts/check-browser-bundle-fresh.mjs
node scripts/check_no_new_python.mjs
git diff --check
```

Add focused reproductions for every suspected defect. Record exact pass/fail
counts and any expected platform skips.

## Required review-file structure

The on-disk findings file must contain:

1. branch/worktree/merge-base verification;
2. verdict: `APPROVE`, `CHANGES REQUESTED`, or `IMPORT-BLOCKING`;
3. findings ordered Critical → High → Medium → Low, each with exact file/line
   evidence, a minimal reproduction, user impact, and remediation;
4. an explicit F-C1…F-L2 resolution table;
5. capability-matrix and spec/task truthfulness audit;
6. validation performed and missing evidence;
7. residual risks and merge recommendation.

If a gap widens or changes the product contract, update
`specs/080-renderable-interchange-import/spec.md` and the capability matrix.
Create concrete unchecked remediation tasks in
`specs/080-renderable-interchange-import/tasks.md`, with owner seams and named
proofs. Do not implement product-code fixes during this review.
