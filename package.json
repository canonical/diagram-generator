{
  "name": "diagram-generator",
  "private": true,
  "scripts": {
    "preview": "npm --prefix apps/preview run start",
    "preview:dev": "npm --prefix apps/preview run dev",
    "preview:build": "npm --prefix apps/preview run build",
    "preview:build-browser": "npm --prefix packages/layout-engine run build:browser",
    "figma-plugin:build": "npm --prefix apps/figma-plugin run build",
    "figma-plugin:serve": "npm --prefix apps/figma-plugin run serve",
    "clean:src-artifacts": "node scripts/clean-src-artifacts.mjs"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
