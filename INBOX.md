
# Inbox

Drop notes here. The agent will triage items into `TODO.md`, then empty this file back to its template header.

it would be nice to be able to multi-select and apply align/distribute to nodes in the force layout; potentially also the elk layout. write a spec-kit spec complete with tasks plan etc, to investigate which algorhithms support it, can it be a global feautre, and if so, creating the ui for it.

unpin on force layout should be availalble with multiple frames selected

unpin works but it doesnt save when i click save; pineed items that were unpinned and saved then revert to pinned